import React from 'react';
import ReactDOM from 'react-dom';
// ReactDOM library helps us to interact with the Document Object Module in the browser
import './index.css'
import App from './App'

ReactDOM.render(
    <React.StrictMode>
        <App />
    </React.StrictMode>,
     document.getElementById('root')
);